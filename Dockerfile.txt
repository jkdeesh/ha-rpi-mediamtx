FROM bluenviron/mediamtx:latest

# Ensure we have a small shell (busybox is already in this image, but this is safe)
# If not needed, you can skip install; most mediamtx images already have /bin/sh.
# For HA add-ons, we just need a script entrypoint.

COPY run.sh /run.sh
RUN chmod +x /run.sh

ENTRYPOINT ["/run.sh"]
